import { TransformErrorMessagePipe } from './transform-error-message.pipe';

describe('TransformErrorMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformErrorMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
