import { AspectRatioHeightPipe } from './aspect-ratio-height.pipe';

describe('AspectRatioHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new AspectRatioHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
