import { ContainerSizePipe } from './container-size.pipe';

describe('ContainerSizePipe', () => {
  it('create an instance', () => {
    const pipe = new ContainerSizePipe();
    expect(pipe).toBeTruthy();
  });
});
