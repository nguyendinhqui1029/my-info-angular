import { ContainerChangeSizeDirective } from './container-change-size.directive';

describe('ContainerChangeSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerChangeSizeDirective();
    expect(directive).toBeTruthy();
  });
});
