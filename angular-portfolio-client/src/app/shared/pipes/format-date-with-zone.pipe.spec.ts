import { FormatDateWithZonePipe } from './format-date-with-zone.pipe';

describe('FormatDateWithZonePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateWithZonePipe();
    expect(pipe).toBeTruthy();
  });
});
